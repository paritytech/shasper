stages:
  - test

image:                             parity/rust:nightly

variables:
  CI_SERVER_NAME:                  "GitLab CI"
  CARGO_HOME:                      "${CI_PROJECT_DIR}/.cargo"
  RUST_TOOLCHAIN:                  "stable"


test-rust-stable:
  stage:                           test
  script:
    - echo "${CI_COMMIT_REF_NAME} tested"
  tags:
    - rust-stable

#   only:
#     - /^[0-9]+$/

